version: 2
models:
  - name: raw_shopify_refunds
    description: "Raw Shopify refund records."
    config:
      tags: ["raw", "shopify"]
    columns:
      - name: refund_id
        description: "Refund PK"
        tests:
          - unique
          - not_null
      - name: order_id
        description: "FK to orders"
        tests:

      - name: created_at
        description: "Refund timestamp"
        tests:

      - name: refund_note
        description: "Internal note"
        tests:

