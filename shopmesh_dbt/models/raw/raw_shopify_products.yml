version: 2
models:
  - name: raw_shopify_products
    description: "Raw Shopify product catalog."
    config:
      tags: ["raw", "shopify"]
    columns:
      - name: product_id
        description: "Product PK"
        tests:
          - unique
          - not_null
      - name: title
        description: "Product title"
        tests:

      - name: vendor
        description: "Vendor name"
        tests:

      - name: product_type
        description: "Product category"
        tests:

      - name: created_at
        description: "Creation timestamp"
        tests:

      - name: status
        description: "active/draft/archived"
        tests:

      - name: product_tags
        description: "Product tags"
        tests:

