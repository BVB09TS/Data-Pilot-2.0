version: 2
models:
  - name: raw_salesforce_accounts
    description: "Raw Salesforce account (company) records."
    config:
      tags: ["raw", "salesforce"]
    columns:
      - name: account_id
        description: "Account PK"
        tests:
          - unique
          - not_null
      - name: account_name
        description: "Company name"
        tests:

      - name: industry
        description: "Industry"
        tests:

      - name: annual_revenue_usd
        description: "Self-reported revenue"
        tests:

      - name: created_at
        description: "Creation timestamp"
        tests:

      - name: account_type
        description: "Customer/Partner/Prospect"
        tests:

