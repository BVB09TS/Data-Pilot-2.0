version: 2
models:
  - name: raw_erp_products
    description: "DEPRECATED: Legacy ERP products. Source decommissioned 2024-01-01. Use raw_shopify_products."
    config:
      tags: ["raw", "legacy_erp"]
    columns:
      - name: prod_id
        description: "Legacy product ID"
        tests:

      - name: prod_name
        description: "Legacy name"
        tests:

      - name: unit_cost
        description: "Unit cost"
        tests:

      - name: supplier_code
        description: "Supplier code"
        tests:

      - name: last_updated
        description: "Last updated"
        tests:

