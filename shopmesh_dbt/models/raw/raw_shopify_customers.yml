version: 2
models:
  - name: raw_shopify_customers
    description: "Raw Shopify customer records."
    config:
      tags: ["raw", "shopify"]
    columns:
      - name: customer_id
        description: "Customer PK"
        tests:
          - unique
          - not_null
      - name: email
        description: "Email address"
        tests:

      - name: created_at
        description: "Account creation timestamp"
        tests:

      - name: total_spent
        description: "Lifetime spend string"
        tests:

      - name: orders_count
        description: "Total orders"
        tests:

      - name: customer_tags
        description: "Shopify tags"
        tests:

      - name: first_name
        description: "First name"
        tests:

      - name: last_name
        description: "Last name"
        tests:

      - name: verified_email
        description: "Email verified flag"
        tests:

