version: 2
models:
  - name: raw_email_campaigns
    description: "Raw email campaign definitions."
    config:
      tags: ["raw", "email_tool"]
    columns:
      - name: campaign_id
        description: "Campaign PK"
        tests:
          - unique
          - not_null
      - name: campaign_name
        description: "Display name"
        tests:

      - name: subject_line
        description: "Email subject"
        tests:

      - name: sent_at
        description: "Send timestamp"
        tests:

      - name: campaign_type
        description: "promotional/transactional/newsletter"
        tests:

      - name: total_sent
        description: "Emails sent"
        tests:

