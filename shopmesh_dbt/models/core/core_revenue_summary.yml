version: 2
models:
  - name: core_revenue_summary
    description: "PLANTED — Monthly revenue for Finance. DUPLICATE METRIC: total_revenue = net only. Conflicts with analytics_revenue_v1 (gross) and analytics_executive_kpis (net+subs). MISSING uniqueness test on revenue_month."
    config:
      tags: ["core"]
    columns:
      - name: revenue_month
        description: "Monthly grain — MISSING uniqueness test"
      - name: total_revenue
        description: "DEFINITION 1: net_revenue only — conflicts with other models"
      - name: gross_revenue
        description: "Sum of order_total_amount"
