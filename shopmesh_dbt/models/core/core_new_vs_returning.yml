version: 2
models:
  - name: core_new_vs_returning
    description: "Classifies each order as new or returning customer purchase."
    config:
      tags: ["core"]
    columns:
      - name: order_id
        description: "Order PK"
        tests:
          - unique
          - not_null
      - name: customer_type
        description: "new (first ever order) / returning"
