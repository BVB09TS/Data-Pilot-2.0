version: 2
models:
  - name: core_geographic_revenue
    description: "Revenue by geography (approximate via email domain heuristics)."
    config:
      tags: ["core"]
    columns:
      - name: approx_country
        description: "ISO country code — APPROXIMATE via email domain"
      - name: gross_revenue
        description: "Sum of gross order revenue"
