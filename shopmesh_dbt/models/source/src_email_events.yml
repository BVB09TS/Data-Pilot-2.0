version: 2
models:
  - name: src_email_events
    description: "Email engagement events with typed timestamps."
    config:
      tags: ["source"]
    columns:
      - name: event_id
        description: "Event PK"
        tests:
          - unique
          - not_null
      - name: event_type
        description: "sent/opened/clicked/bounced/unsubscribed"
