version: 2
models:
  - name: src_inventory_snapshots
    description: "Daily inventory snapshots with typed quantities."
    config:
      tags: ["source"]
    columns:
      - name: snapshot_id
        description: "Snapshot PK"
        tests:
          - unique
          - not_null
      - name: snapshot_date
        description: "Inventory date"
      - name: quantity_on_hand
        description: "Typed integer units"
