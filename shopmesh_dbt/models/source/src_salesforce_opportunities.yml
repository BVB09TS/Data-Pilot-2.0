version: 2
models:
  - name: src_salesforce_opportunities
    description: "Salesforce pipeline opportunities with typed fields."
    config:
      tags: ["source"]
    columns:
      - name: opportunity_id
        description: "Opportunity PK"
        tests:
          - unique
          - not_null
      - name: stage
        description: "Pipeline stage"
      - name: opportunity_value
        description: "Deal value USD"
