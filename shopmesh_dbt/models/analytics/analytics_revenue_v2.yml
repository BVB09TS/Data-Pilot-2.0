version: 2
models:
  - name: analytics_revenue_v2
    description: "DUPLICATE METRIC — total_revenue DEFINITION 3: gross + subs. Uses core_revenue_combined which has the wrong grain join. Inherits inflated subscription revenue."
    config:
      tags: ["analytics"]
    columns:
      - name: revenue_date
        description: "Daily date"
      - name: total_revenue
        description: "DEFINITION 3: order + subs (INFLATED via grain bug)"
      - name: subscription_revenue
        description: "INFLATED: full month MRR per day"
