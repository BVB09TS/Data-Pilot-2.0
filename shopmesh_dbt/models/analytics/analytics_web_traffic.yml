version: 2
models:
  - name: analytics_web_traffic
    description: "Web traffic summary by channel and device."
    config:
      tags: ["analytics"]
    columns:
      - name: session_date
        description: "Date of sessions"
      - name: engagement_rate
        description: "engaged_sessions / session_count"
