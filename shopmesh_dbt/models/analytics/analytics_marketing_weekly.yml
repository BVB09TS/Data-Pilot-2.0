version: 2
models:
  - name: analytics_marketing_weekly
    description: "Weekly paid media performance summary."
    config:
      tags: ["analytics"]
    columns:
      - name: report_week
        description: "Week start date"
      - name: total_spend
        description: "Total paid media spend"
      - name: blended_cpa
        description: "ad_spend / conversions"
