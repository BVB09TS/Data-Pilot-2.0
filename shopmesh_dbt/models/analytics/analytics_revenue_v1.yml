version: 2
models:
  - name: analytics_revenue_v1
    description: "DUPLICATE METRIC — total_revenue DEFINITION 2: gross orders only. Marketing uses this. Conflicts with core_revenue_summary (net) and analytics_executive_kpis (net+subs)."
    config:
      tags: ["analytics"]
    columns:
      - name: revenue_date
        description: "Daily revenue date"
      - name: total_revenue
        description: "DEFINITION 2: gross_revenue only — Marketing definition"
