version: 2
models:
  - name: analytics_geographic_revenue
    description: "Revenue by geography for executive team."
    config:
      tags: ["analytics"]
    columns:
      - name: country_code
        description: "ISO country code (approximate)"
      - name: revenue_share_pct
        description: "Country / total gross revenue"
