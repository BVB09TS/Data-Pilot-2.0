version: 2
models:
  - name: analytics_subscription_health
    description: "Subscription KPIs for growth team, by plan and status."
    config:
      tags: ["analytics"]
    columns:
      - name: plan_id
        description: "Plan identifier"
      - name: total_mrr
        description: "MRR for plan+status"
      - name: avg_age_days
        description: "Average subscription age"
