version: 2
models:
  - name: analytics_churn_risk
    description: "DEAD MODEL: Superseded by analytics_customer_health which includes churn_status. Last queried 2024-02-10. Still costing $28/month."
    config:
      tags: ["analytics"]
    columns:
      - name: customer_id
        description: "Customer PK"
      - name: churn_risk_level
        description: "REDUNDANT — use analytics_customer_health.churn_status"
