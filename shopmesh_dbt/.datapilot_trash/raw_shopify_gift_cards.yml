version: 2
models:
  - name: raw_shopify_gift_cards
    description: "ORPHANED: Gift card data. Gift card feature was paused Q3 2023. No downstream models."
    config:
      tags: ["raw", "shopify"]
    columns:
      - name: gift_card_id
        description: "Gift card ID"
        tests:

      - name: code
        description: "Redemption code"
        tests:

      - name: balance
        description: "Remaining balance"
        tests:

      - name: created_at
        description: "Issue timestamp"
        tests:

      - name: expires_on
        description: "Expiry date"
        tests:

