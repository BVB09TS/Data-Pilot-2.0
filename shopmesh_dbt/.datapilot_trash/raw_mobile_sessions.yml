version: 2
models:
  - name: raw_mobile_sessions
    description: "ORPHANED: Mobile session aggregations. Superseded by deriving sessions from raw_mobile_events. No downstream refs."
    config:
      tags: ["raw", "mobile_app"]
    columns:
      - name: session_id
        description: "Session ID"
        tests:

      - name: user_id
        description: "User ID"
        tests:

      - name: started_at
        description: "Session start"
        tests:

      - name: ended_at
        description: "Session end"
        tests:

      - name: event_count
        description: "Events in session"
        tests:

